---
description: Git 提交规范。一次提交只做一件事；中文说明配英文前缀；复杂改动用标题加条目；未明确要求时不自动提交。
alwaysApply: true
---

# 版本控制

## 何时提交

未收到明确要求时，不要自动创建提交。

## 提交粒度

一次提交只聚焦一个明确改动，不混入无关修改。

## 提交信息

使用中文正文，并以英文前缀开头：`<前缀>: `。需要时加 scope：`<前缀>(<scope>): `。

常用前缀：`feat`、`fix`、`refactor`、`chore`、`docs`。

简单改动：一行标题即可。

复杂改动按以下结构书写：

1. 第一条为概要标题
2. 随后 1–10 条子项，说明具体改动
3. 特别重要的改动或重大问题单独成段列出

```
feat: 接入本地草稿缓存

- 新增草稿 Provider 与本地缓存
- 编辑页支持离开后恢复

注意：旧草稿格式不兼容，首次打开会清空。
```
